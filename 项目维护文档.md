# 邮件归档管理系统（mail-archiver）维护文档

## 一、项目基础信息

- 项目名称：邮件归档管理系统（mail-archiver）
- 启动时间：2026-09-18（文档建立日，项目实际启动时间待补充）
- 技术栈：Python 3.8+（零外部依赖，纯标准库实现）
- 项目框架：单体 CLI / 内置 HTTP Web 服务，单包结构 `mail_archiver/`
- 参与人员：待补充

## 二、业务名词表

| 名词 | 含义 | 备注 |
|---|---|---|
| 归档 | 把邮件按「日期→发件人→每封」结构落盘到本地文件夹 | 核心动作 |
| 授权码 | 163 等邮箱网页端生成的 IMAP 访问凭证 | 不是登录密码，需在邮箱设置中开启 IMAP 后生成 |
| IMAP | 邮件接收协议，工具用它连邮箱拉信 | 默认 imap.163.com:993 SSL |
| Message-ID | 邮件唯一标识，用于去重 | 无 ID 时用 SHA256(raw) 生成 `<digest@local.generated>` |
| 去重 | 同一封邮件不重复归档 | 索引存 `.archive_index.json` |
| 邮件预览 | 每封邮件渲染成的 PNG 截图 | 调用本机 Edge/Chrome headless 生成，存为 `邮件预览.png` |
| 日报 | 每天的归档清单 CSV | 文件名 `_日报.csv`，按日期目录存放 |
| 自检 | 内置样例邮件走通归档流程并校验 | `python -m mail_archiver --self-test` |
| 内嵌图片 | 正文里 `cid:` 引用的图片 | 截图时替换为 data URI 以正确渲染 |

## 三、项目框架说明

### 分层结构

```
入口
  __main__.py        ← python -m mail_archiver
  cli.py             ← 参数解析，分发到 Web / CLI / 自检
  邮件归档.bat        ← Windows 双击启动 Web

配置
  config.py          ← JSON 配置读写，IMAP 域名→主机推断

归档管线（pipeline.py 调度）
  imap_client.py     ← IMAP 连接/登录/按日期搜索/逐封拉取
  local_import.py    ← 本地 .eml / .mbox 导入
  parse.py           ← 原始字节 → ParsedMail（主题/发件人/正文/附件）
  archiver.py        ← 核心：目录生成、去重、写 meta.json、附件落盘、截图、日报
  index.py           ← .archive_index.json 去重索引
  extract.py         ← 附件压缩包自动解压（zip/tar/rar/7z），含路径穿越防护
  report.py          ← _日报.csv 追加
  screenshot.py      ← Edge/Chrome headless 生成邮件预览 PNG
  results.py         ← 按日期列出已归档结果（供 Web 展示）
  util.py            ← 时区、文件名清洗、路径去重、日志配置

Web 层
  webapp.py          ← BaseHTTPRequestHandler 内置 HTTP 服务 + REST API
  jobs.py            ← 后台任务运行器（线程 + JobState 状态机）
  web/index.html     ← 单页前端（原生 HTML/CSS/JS，无框架）

自检
  selftest.py        ← 构造样例邮件→归档→校验目录结构/安全/去重
```

### 关键文件夹

- `mail_archiver/web/` — 前端静态页（单文件）
- `mail_archiver/__pycache__/` — 编译缓存，已 gitignore
- 归档输出目录默认 `~/Desktop/邮件归档/`，可在配置或页面改

### 核心配置

配置文件 `config.local.json`（已 gitignore，不进版本库）：

```json
{
  "archive_root": "D:\\邮件归档测试",
  "timezone": "Asia/Shanghai",
  "imap": {
    "enabled": true,
    "host": "imap.163.com",
    "port": 993,
    "ssl": true,
    "username": "xxx@163.com",
    "auth_code": "授权码",
    "folder": "INBOX"
  },
  "extract": {
    "enabled": true,
    "max_files": 500,
    "max_bytes": 524288000
  },
  "auth_codes": {
    "xxx@163.com": "授权码"
  }
}
```

环境变量可覆盖配置项（优先级最高）：`COMPLAINT_ARCHIVE_ROOT`、`COMPLAINT_IMAP_HOST`、`COMPLAINT_IMAP_PORT`、`COMPLAINT_IMAP_USER`、`COMPLAINT_IMAP_AUTH`、`COMPLAINT_IMAP_FOLDER`。

支持自动推断 IMAP 主机的邮箱域：163/126/yeah、qq/foxmail/vip.qq/exmail.qq、gmail/googlemail、outlook/hotmail/live/msn、icloud/me/mac、sina/sina.cn、aliyun、139、189、sohu、tom、yahoo（见 `config.py` 的 `DOMAIN_INFO`）。未识别的邮箱可在网页「IMAP 服务器」输入框手动填写。

### 启动与部署

1. 安装 Python 3.10+（项目要求 ≥3.8，实际建议 3.10+）
2. Windows 双击 `邮件归档.bat`，或命令行 `python -m mail_archiver`
3. 浏览器访问 http://127.0.0.1:8765/
4. 页面填邮箱+授权码，点「开始整理这一天的邮件」
5. CLI 模式：`python -m mail_archiver --cli --date 2026-09-18`
6. 本地导入：`python -m mail_archiver --from-dir <目录>`
7. 自检：`python -m mail_archiver --self-test`

### 归档目录结构示例

```
邮件归档/
└── 2026-09-16/
    ├── _日报.csv
    ├── 张三zhangsan@example.com/
    │   ├── 无法登录生产环境_001/
    │   │   ├── 邮件预览.png
    │   │   ├── meta.json          ← 隐藏文件
    │   │   └── attachments/
    │   │       ├── 截图.png
    │   │       └── 日志.zip → 自动解压到 日志/
    │   └── 发票请尽快开_002/
    └── 李四lisi@example.com/
        └── 压缩包路径测试_001/
```

## 四、变更记录

| 日期 | 变更说明 |
|---|---|
| 2026-09-21 | 批量导入：网页端选文件上传由「一次性 POST 全部」改为前端按 20 封一批、每批等后台归档任务结束后再传下一批——整包 multipart 会被后端整个读进内存，几百封带附件足以撑爆内置服务器；且任务器同时只跑一个任务，连发会直接报「正在整理中」。状态栏显示逐批进度，导入期间日志面板实时刷新。修复选完文件点「打开」无反应：change 处理器先清空 input 再取文件，而清空会连 FileList 一起清掉，拿到空列表直接返回；改为先拷贝成真数组。配合网易邮箱大师导出的几百封 .eml 场景：文件框本就允许多选（Ctrl+A 全选），命令行另有 `--from-dir` 整目录导入（按信头日期归目录，已归档的邮件去重索引自动跳过）。补无头浏览器回归用例校验分批边界、多选属性与文件交付 |
| 2026-09-21 | 区间导出两处加固：① 候选 UID 从「服务端日期 SEARCH 初筛 + 二次校验」改为按序号 1..EXISTS 全量枚举 (UID INTERNALDATE) 再本地筛区间——各服务商 SEARCH 实现参差（大跨度截断、空范围返回整箱等），序号连续不受影响；② 归档日期优先 Date 信头（即网页/客户端列表显示的发信时间），信头缺失或明显离谱（早于 1990 年、晚于明天，垃圾邮件常伪造）才回退入箱时间。排查「选 6/1~9/21 却从 8 月开始导出」：三轮只读探针确认工具与网页登录的是同一账号，IMAP 协议下该邮箱最早只有 2026-08-22（恰为当天往前 30 天）的邮件，而网页端同箱可见 6/9 起的邮件——163 免费版对第三方客户端 IMAP 只下发最近约 30 天邮件的服务端限制，非程序缺陷。补 SEARCH 截断模拟与信头日期归档两组回归用例，全量自检通过 |
| 2026-09-21 | 修复日历点击失效：日历面板内点 ‹ › 翻月或选日期时，事件冒泡到 date-wrap 的开/关处理器，面板被立刻关闭；重新打开时 openCalendar 又按已选日期重置月份，看起来就是"点了没反应"。根因有两层：面板内点击会冒泡到外层开关；且 renderCalendar 用 innerHTML 重建面板后，点击目标节点已脱离 DOM，事件仍沿派发时路径冒泡而 contains(e.target) 对脱离节点恒为 false，单靠 contains 守卫拦不住。修复：在翻月/选日处理器源头 stopPropagation，外层开关与 document 关闭逻辑都不再误判；顺带"点两个日期选区间"全程无需重开面板。自检新增无头浏览器模拟真实点击的回归用例（验证翻月与区间两连点后面板仍打开、日期值正确） |
| 2026-09-21 | Win7 兼容收尾：① `imaplib` 的 `timeout` 参数是 3.9 才加入的，3.8（Win7 唯一能装的版本）上连接必报 TypeError，改为按 `sys.version_info` 分支——3.8 用全局 `socket.setdefaulttimeout(30)` 兜底，3.9+ 不变；② Win7 上最后的浏览器是 Chrome 109/Edge 110，不认识 `--headless=new`（112 才有），截图测高与出图两处都改为"没有结果就换旧 `--headless` 重试"，不再依赖 returncode 判断。全量自检通过 |
| 2026-09-20 | 兼容 Python 3.8（Win7 部署）：目标机器只有 Win7、装不了 3.9+（3.9 起 IMAP 官方要求 Win8.1+），而项目唯一用到 3.9 特性的是 util.py 的 zoneinfo。改为 try-import，3.8 下自动走既有的 UTC+8 固定偏移兜底（中国无夏令时，行为等价），3.9+ 主路径不变。实测兜底路径与 3.12 完整自检均通过。最低版本要求回到 pyproject 声明的 3.8 |
| 2026-09-18 | 修复 INTERNALDATE 时间解释：原实现交给 RFC 2822 解析器，不带时区偏移的裸值（部分服务器返回）会解析失败，导致邮件被区间筛选整个丢掉。改为专用解析器：带偏移按偏移转换，无偏移按 RFC 3501 视为服务器本地时间（中文邮箱即北京时间），由 to_tz 按配置时区解释——16:00 之后收到的邮件不会被错算到第二天。补自检用例覆盖裸值解析与区间不丢信 |
| 2026-09-18 | 日期改为区间选择：日历点两个日期即选区间（同一天=单日），区间内所有邮件一次导出。后端 `fetch_in_range` 用 `SINCE start BEFORE end+1` 一次 SEARCH 再按 INTERNALDATE 二次校验；`list_range` 聚合多日结果；`/api/state` 与 `/api/run` 收 `date_start`/`date_end`；CLI 加 `--start`/`--end`；前端日历高亮区间、按钮文案与结果计数随单日/区间切换；补区间筛选与区间聚合自检 |
| 2026-09-18 | 切换邮箱时授权码跟随切换更稳健：`maybeFillAuth` 把「清旧码」改为切换时同步执行（不再依赖 `/api/saved-auth` 异步回调），杜绝服务未重启时拿 A 邮箱的码登 B；异步只负责带入已存码，且仅在框为空时填入、不覆盖用户手输 |
| 2026-09-18 | 按邮箱记忆授权码：`config.local.json` 新增 `auth_codes` 字典，保存时按邮箱写入/更新；新增 `get_saved_auth_code` 与 `GET /api/saved-auth` 接口；前端输入已识别邮箱后自动带入其已存授权码，重新输入则更新，切换邮箱不串码 |
| 2026-09-18 | 输入框样式：未选中背景统一为白色（移除只读框的黄色 `#f3efe7` 底），文字左对齐，聚焦时改为蓝色描边 + 光晕，日期框悬停色调同步为浅蓝 |
| 2026-09-18 | IMAP 服务器输入框改为只读，纯按邮箱后缀自动填充，不可手动编辑；切换服务商时清空旧授权码并提示重填 |
| 2026-09-18 | 邮箱识别优化：扩充域名表至 25 个，新增 `infer_imap_info`；网页加「IMAP 服务器」输入框与实时识别反馈；`save_config` 支持 host 覆盖；补自检用例；去除页面写死的 163 文案 |
| 2026-09-18 | 完善 README：补全功能特性、命令行参数、配置说明、目录结构与项目结构，并推送 GitHub |
| 2026-09-18 | 初始化项目维护文档；初始化 Git 仓库并首次提交；同步至 GitHub |
